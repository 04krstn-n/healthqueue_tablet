import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1A6BC4);
  static const secondary = Color(0xFF26A69A);
  static const background = Color(0xFFF2F6FB);
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFE1E7EF);
  static const textPrimary = Color(0xFF121F3A);
  static const textSecondary = Color(0xFF667085);
  static const textMuted = Color(0xFF94A3B8);
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFDC2626);

  // Backwards-compatible aliases used across the app
  static const textDark = textPrimary;
  static const bgTop = primary;
  static const bgBottom = secondary;
}
